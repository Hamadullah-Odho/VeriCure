/*
 * ============================================================
 * API CONFIG
 * ============================================================
 *
 * CHANGE THIS ONE LINE ONLY.
 *
 * Every screen imports API_BASE_URL from here, so updating
 * your laptop's IP address in this single file updates it
 * everywhere in the app.
 *
 * How to find your IP:
 *
 * Windows -> ipconfig            -> look for "IPv4 Address"
 * Mac     -> ifconfig | grep inet
 *
 * Your phone and laptop must be on the SAME Wi-Fi network.
 */

export const API_BASE_URL = 'http://192.168.100.15:8080';